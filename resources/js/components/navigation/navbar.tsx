'use client';

import MaxWidthWrapper from '@/components/global/max-width-wrapper';

const Navbar = () => {
    return (
        <header id="navbar">
            <MaxWidthWrapper>Navbar</MaxWidthWrapper>
        </header>
    );
};

export { Navbar };
